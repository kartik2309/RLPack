mode: 'train'
num_episodes: 1500
max_timesteps: 2000
model_name: 'dqn1d'

model_args: {
  num_actions: 4,
  activation: 'relu',
  sequence_length: 2,
  channels: [ 4, 16, 32 ],
  kernel_sizes_conv: [ 1, 1 ],
  strides_sizes_conv: [ 1, 1 ],
  dilation_sizes_conv: [ 1, 1 ],
  kernel_sizes_pool: null,
  strides_sizes_pool: null,
  dilation_sizes_pool: null,
  use_padding: True,
  dropout: 0.5
}

activation_args: {
}

agent_args: {
  gamma: 0.9,
  epsilon: 0.5,
  num_actions: 4,
  memory_buffer_size: 256,
  target_model_update_rate: 128,
  policy_model_update_rate: 32,
  epsilon_decay_rate: 0.8,
  save_path: "/Users/kartikrajeshwaran/CodeSupport/CPP/Models/RLPack/dqn1d/",
}

optimizer_args: {
  optimizer: "adam",
  lr: 5e-2,
}

device: 'cpu'