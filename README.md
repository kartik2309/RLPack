# RLPack
Implementation of RL Algorithms with C++ Backend. 

This project aims to build a lightweight library with C++ backend for RL Algorithms. 
RL Algorithms can be interacted with `gym` environment via Python interface.  
